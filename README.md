# Sprint 0 – Python Environment Setup

This repository demonstrates my Python development environment setup for Sprint 0.

## What this repository shows
- Python installed and runnable from the command line
- Unit testing using Python's `unittest` framework
- Git version control with code hosted on GitHub
- Development using Visual Studio Code

## How to run the unit tests
From the project directory, run:

```bash
python -m unittest -v
